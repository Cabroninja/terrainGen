services:
  terrain-generator:
    build: .
    container_name: jkr-terrain-generator-v2
    restart: unless-stopped
    ports:
      - "8080:8000"
    environment:
      OUTPUT_DIR: /app/output
    volumes:
      - terrain_outputs:/app/output
volumes:
  terrain_outputs:
